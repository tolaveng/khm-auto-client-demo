import React from 'react';

const App: React.FunctionComponent = () => {
    return (
        <div className="app">
            <h1>React running in Electron App :)</h1>
        </div>
    );
};

export default App;
